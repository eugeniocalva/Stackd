
==================================================
Step 81 (replace_file_content)
Description: "Update label Total Portfolio to Net Worth in AnalyticsView to prevent confusion."
Instruction: "Change Total Portfolio to Net Worth in AnalyticsView layout"
StartLine: 93, EndLine: 97
TargetContent:
"            <p class=\"section-title\" style=\"margin-bottom: var(--space-1); text-transform: uppercase; letter-spacing: 0.05em; font-size: 0.75rem; opacity: 0.8;\">Total Portfolio</p>"
ReplacementContent:
"            <p class=\"section-title\" style=\"margin-bottom: var(--space-1); text-transform: uppercase; letter-spacing: 0.05em; font-size: 0.75rem; opacity: 0.8;\">Net Worth</p>"

==================================================
Step 85 (replace_file_content)
Description: "Redesign PortfolioView with pull-to-refresh, left-aligned header, right-aligned stock list items, performance toggle, color coding, and cleanup logic."
Instruction: "Replace window.Views.PortfolioView with updated implementation including pull-to-refresh, left-aligned header, stock list redesign, and performance toggle."
StartLine: 4113, EndLine: 4396
TargetContent:
"window.Views.PortfolioView = {\n  render(state) {\n    const holdings = state.portfolioHoldings || [];\n    const hasKey = !!window.StackdMarket.getApiKey();\n\n    // Calculate portfolio values using market service\n    const portfolioVal = window.StackdMarket.computePortfolioValue() || {\n      totalMarketValue: 0,\n      totalInvested: 0,\n      totalRealizedPnL: 0,\n      totalUnrealizedPnL: 0,\n      totalUnrealizedPnLPct: 0,\n      totalPnL: 0,\n      positions: []\n    };\n\n    // Overall portfolio stats\n    const totalVal = portfolioVal.totalMarketValue;\n    const totalInvested = portfolioVal.totalInvested;\n    const totalPnL = portfolioVal.totalPnL;\n    \n    const pnlPercent = totalInvested > 0 ? (totalPnL / totalInvested) * 100 : 0;\n    const pnlSign = totalPnL >= 0 ? '+' : '';\n    const pnlColor = totalPnL >= 0 ? 'var(--color-income-val)' : 'var(--color-expense)';\n    const pnlBgColor = totalPnL >= 0 ? 'var(--color-income-bg)' : 'var(--color-expense-bg)';\n\n    // Build holdings list\n    let holdingsHtml = '';\n    if (holdings.length === 0) {\n      holdingsHtml = `\n        <div style=\"text-align: center; padding: var(--space-8) var(--space-4); background: var(--bg-surface); border-radius: var(--radius-xl); border: 1px dashed var(--border-color); margin-top: var(--space-6);\">\n          <div style=\"font-size: 3rem; margin-bottom: var(--space-4);\">📈</div>\n          <h3 style=\"margin-bottom: var(--space-2); color: var(--text-primary);\">No Assets Logged</h3>\n          <p style=\"font-size: 0.85rem; color: var(--text-secondary); max-width: 250px; margin: 0 auto var(--space-6) auto; line-height: 1.5;\">\n            Track your stocks, ETFs, and crypto investments in real-time.\n          </p>\n          <a href=\"#edit-asset\" class=\"btn btn-primary\" style=\"display: inline-block; width: auto; padding: var(--space-3) var(--space-6); font-weight: 700; border-radius: var(--radius-lg);\">Add Your First Asset</a>\n        </div>\n      `;\n    } else {\n      holdingsHtml = portfolioV
<truncated 16384 bytes>
ReplacementContent:
"window.Views.PortfolioView = {\n  render(state) {\n    const holdings = state.portfolioHoldings || [];\n    const hasKey = !!window.StackdMarket.getApiKey();\n\n    // Calculate portfolio values using market service\n    const portfolioVal = window.StackdMarket.computePortfolioValue() || {\n      totalMarketValue: 0,\n      totalInvested: 0,\n      totalRealizedPnL: 0,\n      totalUnrealizedPnL: 0,\n      totalUnrealizedPnLPct: 0,\n      totalPnL: 0,\n      positions: []\n    };\n\n    // Overall portfolio stats\n    const totalVal = portfolioVal.totalMarketValue;\n    const totalInvested = portfolioVal.totalInvested;\n    const totalPnL = portfolioVal.totalPnL;\n    \n    const pnlPercent = totalInvested > 0 ? (totalPnL / totalInvested) * 100 : 0;\n    \n    // Performance view toggle state\n    const performanceView = localStorage.getItem('stackd_portfolio_perf_view') || 'percentage';\n    const isPercentage = performanceView === 'percentage';\n    \n    const pnlSign = totalPnL >= 0 ? '▲ ' : '▼ ';\n    const pnlText = isPercentage \n      ? `${pnlPercent.toFixed(2)}%` \n      : window.Store.formatCurrency(Math.abs(totalPnL));\n      \n    const pnlColor = totalPnL >= 0 ? 'var(--color-income-val)' : 'var(--color-expense)';\n    const pnlBgColor = totalPnL >= 0 ? 'var(--color-income-bg)' : 'var(--color-expense-bg)';\n\n    // Build holdings list\n    let holdingsHtml = '';\n    if (holdings.length === 0) {\n      holdingsHtml = `\n        <div style=\"text-align: center; padding: var(--space-8) var(--space-4); background: var(--bg-surface); border-radius: var(--radius-xl); border: 1px dashed var(--border-color); margin-top: var(--space-6);\">\n          <div style=\"font-size: 3rem; margin-bottom: var(--space-4);\">📈</div>\n          <h3 style=\"margin-bottom: var(--space-2); color: var(--text-primary);\">No Assets Logged</h3>\n          <p style=\"font-size: 0.85rem; color: var(--text-secondary); max-width: 250px; margin: 0 auto var(--space-6) auto; line-height: 1.5;\">\n            Track your stocks, E
<truncated 21945 bytes>

==================================================
Step 182 (replace_file_content)
Description: "Adds the four missing portfolio-related views. PortfolioView has a left-aligned large header (portfolio value + P&L), pull-to-refresh gesture support, a search bar, a performance toggle (% / absolute), per-asset cards with current price, market value, and daily change, plus auto-refresh on mount when cache is stale. EditAssetView handles both create and edit of a holding with live ticker search. SellAssetView handles partial/full exits. MarketSettingsView provides Finnhub API key management and cache controls."
Instruction: "Append PortfolioView, EditAssetView, SellAssetView, and MarketSettingsView to the end of views.js."
StartLine: 3216, EndLine: 3217
TargetContent:
"});\n"
ReplacementContent:
"});\n\n// ─────────────────────────────────────────────────────────\n//  PORTFOLIO VIEW\n// ─────────────────────────────────────────────────────────\nwindow.Views.PortfolioView = {\n  render(state) {\n    const holdings = state.portfolioHoldings || [];\n    const hasKey = !!(window.StackdMarket && window.StackdMarket.getApiKey());\n    const marketStatus = state.marketStatus || {};\n\n    // Calculate portfolio values using market service\n    const portfolioVal = (window.StackdMarket && window.StackdMarket.computePortfolioValue()) || {\n      totalMarketValue: 0,\n      totalInvested: 0,\n      totalRealizedPnL: 0,\n      totalUnrealizedPnL: 0,\n      totalUnrealizedPnLPct: 0,\n      totalPnL: 0,\n      positions: []\n    };\n\n    const totalVal      = portfolioVal.totalMarketValue;\n    const totalInvested = portfolioVal.totalInvested;\n    const totalPnL      = portfolioVal.totalPnL;\n    const pnlPercent    = totalInvested > 0 ? (totalPnL / totalInvested) * 100 : 0;\n\n    // Performance view toggle state (persisted to localStorage)\n    const performanceView = localStorage.getItem('stackd_portfolio_perf_view') || 'percentage';\n    const isPercentage    = performanceView === 'percentage';\n\n    const pnlSign  = totalPnL >= 0 ? '▲ ' : '▼ ';\n    const pnlText  = isPercentage\n      ? `${pnlSign}${Math.abs(pnlPercent).toFixed(2)}%`\n      : `${pnlSign}${window.Store.formatCurrency(Math.abs(totalPnL))}`;\n\n    const pnlColor   = totalPnL >= 0 ? 'var(--color-income-val)'  : 'var(--color-expense)';\n    const pnlBgColor = totalPnL >= 0 ? 'var(--color-income-bg)'   : 'var(--color-expense-bg)';\n\n    // Oldest price age badge\n    const tickers       = holdings.map(h => h.ticker);\n    const oldestAge     = (window.StackdMarket && tickers.length) ? window.StackdMarket.getOldestPric
<truncated 46526 bytes>

==================================================
Step 229 (replace_file_content)
Description: "Update Analytics View card label from Total Portfolio to Net Worth"
Instruction: "Change 'Total Portfolio' to 'Net Worth' in AnalyticsView render output"
StartLine: 90, EndLine: 100
TargetContent:
"            <p class=\"section-title\" style=\"margin-bottom: var(--space-1); text-transform: uppercase; letter-spacing: 0.05em; font-size: 0.75rem; opacity: 0.8;\">Total Portfolio</p>"
ReplacementContent:
"            <p class=\"section-title\" style=\"margin-bottom: var(--space-1); text-transform: uppercase; letter-spacing: 0.05em; font-size: 0.75rem; opacity: 0.8;\">Net Worth</p>"

==================================================
Step 231 (replace_file_content)
Description: "Redesign PortfolioView to add the performance toggle select dropdown, convert stock prices to base currency, and make pull-to-refresh inertia/scrolling updates robust."
Instruction: "Replace window.Views.PortfolioView definition in views.js with the updated version"
StartLine: 3218, EndLine: 3552
TargetContent:
"// ─────────────────────────────────────────────────────────\n//  PORTFOLIO VIEW\n// ─────────────────────────────────────────────────────────\nwindow.Views.PortfolioView = {\n  render(state) {\n    const holdings = state.portfolioHoldings || [];\n    const hasKey = !!(window.StackdMarket && window.StackdMarket.getApiKey());\n    const marketStatus = state.marketStatus || {};\n\n    // Calculate portfolio values using market service\n    const portfolioVal = (window.StackdMarket && window.StackdMarket.computePortfolioValue()) || {\n      totalMarketValue: 0,\n      totalInvested: 0,\n      totalRealizedPnL: 0,\n      totalUnrealizedPnL: 0,\n      totalUnrealizedPnLPct: 0,\n      totalPnL: 0,\n      positions: []\n    };\n\n    const totalVal      = portfolioVal.totalMarketValue;\n    const totalInvested = portfolioVal.totalInvested;\n    const totalPnL      = portfolioVal.totalPnL;\n    const pnlPercent    = totalInvested > 0 ? (totalPnL / totalInvested) * 100 : 0;\n\n    // Performance view toggle state (persisted to localStorage)\n    const performanceView = localStorage.getItem('stackd_portfolio_perf_view') || 'percentage';\n    const isPercentage    = performanceView === 'percentage';\n\n    const pnlSign  = totalPnL >= 0 ? '▲ ' : '▼ ';\n    const pnlText  = isPercentage\n      ? `${pnlSign}${Math.abs(pnlPercent).toFixed(2)}%`\n      : `${pnlSign}${window.Store.formatCurrency(Math.abs(totalPnL))}`;\n\n    const pnlColor   = totalPnL >= 0 ? 'var(--color-income-val)'  : 'var(--color-expense)';\n    const pnlBgColor = totalPnL >= 0 ? 'var(--color-income-bg)'   : 'var(--color-expense-bg)';\n\n    // Oldest price age badge\n    const tickers       = holdings.map(h => h.ticker);\n    const oldestAge     = (window.StackdMarket && tickers.length) ? window.StackdMarket.getOldestPriceAge(ti
<truncated 15645 bytes>
ReplacementContent:
"// ─────────────────────────────────────────────────────────\n//  PORTFOLIO VIEW\n// ─────────────────────────────────────────────────────────\nwindow.Views.PortfolioView = {\n  render(state) {\n    const holdings = state.portfolioHoldings || [];\n    const hasKey = !!(window.StackdMarket && window.StackdMarket.getApiKey());\n    const marketStatus = state.marketStatus || {};\n\n    // Calculate portfolio values using market service\n    const portfolioVal = (window.StackdMarket && window.StackdMarket.computePortfolioValue()) || {\n      totalMarketValue: 0,\n      totalInvested: 0,\n      totalRealizedPnL: 0,\n      totalUnrealizedPnL: 0,\n      totalUnrealizedPnLPct: 0,\n      totalPnL: 0,\n      positions: []\n    };\n\n    const totalVal      = portfolioVal.totalMarketValue;\n    const totalInvested = portfolioVal.totalInvested;\n    const totalPnL      = portfolioVal.totalPnL;\n    const pnlPercent    = totalInvested > 0 ? (totalPnL / totalInvested) * 100 : 0;\n\n    // Performance view toggle state (persisted to localStorage)\n    const performanceView = localStorage.getItem('stackd_portfolio_perf_view') || 'percentage';\n    const isPercentage    = performanceView === 'percentage';\n\n    const pnlSign  = totalPnL >= 0 ? '▲ ' : '▼ ';\n    const pnlText  = isPercentage\n      ? `${pnlSign}${Math.abs(pnlPercent).toFixed(2)}%`\n      : `${pnlSign}${window.Store.formatCurrency(Math.abs(totalPnL))}`;\n\n    const pnlColor   = totalPnL >= 0 ? 'var(--color-income-val)'  : 'var(--color-expense)';\n    const pnlBgColor = totalPnL >= 0 ? 'var(--color-income-bg)'   : 'var(--color-expense-bg)';\n\n    // Oldest price age badge\n    const tickers       = holdings.map(h => h.ticker);\n    const oldestAge     = (window.StackdMarket && tickers.length) ? window.StackdMarket.getOldestPriceAge(ti
<truncated 17845 bytes>

==================================================
Step 300 (replace_file_content)
Description: "Update individual holding card markup in views.js to wrap it in a swipe-container and format the P&L gain/loss string with explicit plus/minus signs."
Instruction: "Replace stock card rendering html and pnlLine formatting to support swipe delete wrapper and proper signs for losses"
StartLine: 3336, EndLine: 3371
TargetContent:
"        // Unrealized P&L line\n        let pnlLine = '';\n        if (!isSoldOut && pos.unrealizedPnL != null) {\n          const upSign  = pos.unrealizedPnL >= 0 ? '+' : '';\n          const upColor = pos.unrealizedPnL >= 0 ? 'var(--color-income-val)' : 'var(--color-expense)';\n          pnlLine = `<span style=\"font-size:0.75rem; color:${upColor}; font-weight:600;\">${upSign}${window.Store.formatCurrency(pos.unrealizedPnL)} (${upSign}${(pos.unrealizedPnLPct || 0).toFixed(1)}%)</span>`;\n        }\n\n        const isFallback = pos.isFallback;\n\n        return `\n          <a href=\"#edit-asset?id=${holding.id}\" class=\"portfolio-asset-card\" data-ticker=\"${holding.ticker}\" data-name=\"${(holding.name || '').toLowerCase()}\" style=\"display:flex; align-items:center; gap:var(--space-3); padding:var(--space-4) var(--space-4); background:var(--bg-surface); border-radius:var(--radius-xl); margin-bottom:var(--space-3); border:1px solid var(--border-color); text-decoration:none; color:inherit; cursor:pointer; transition:transform 0.15s;\">\n            <!-- Ticker badge -->\n            <div style=\"width:46px; height:46px; border-radius:var(--radius-lg); background:var(--bg-surface-sunken); display:flex; align-items:center; justify-content:center; flex-shrink:0; overflow:hidden;\">\n              <span style=\"font-size:0.6rem; font-weight:800; letter-spacing:0.02em; color:var(--text-secondary); text-align:center; line-height:1.2; padding:2px;\">${holding.ticker.slice(0,5)}</span>\n            </div>\n            <!-- Info -->\n            <div style=\"flex:1; min-width:0;\">\n              <div style=\"display:flex; align-items:center; gap:var(--space-2); margin-bottom:2px;\">\n                <span style=\"font-weight:700; font-size:0.95rem; color:var(--text-primary); white-space:nowrap; overflow:hidden; text-overflow:ellipsis; max-width:140px;\">${holding.name || holding.ticker}</span>\n                ${isFallback ? `<i data-lucide=\"alert-circle\" style=\"width:12px; height:12px; color:var(--text-tertiary);
<truncated 807 bytes>
ReplacementContent:
"        // Unrealized P&L line\n        let pnlLine = '';\n        if (!isSoldOut && pos.unrealizedPnL != null) {\n          const upSign  = pos.unrealizedPnL >= 0 ? '+' : '-';\n          const pctSign = pos.unrealizedPnLPct >= 0 ? '+' : '-';\n          const upColor = pos.unrealizedPnL >= 0 ? 'var(--color-income-val)' : 'var(--color-expense)';\n          pnlLine = `<span style=\"font-size:0.75rem; color:${upColor}; font-weight:600;\">${upSign}${window.Store.formatCurrency(Math.abs(pos.unrealizedPnL))} (${pctSign}${Math.abs(pos.unrealizedPnLPct || 0).toFixed(1)}%)</span>`;\n        }\n\n        const isFallback = pos.isFallback;\n\n        return `\n          <div class=\"swipe-container\" data-id=\"${holding.id}\">\n            <div class=\"swipe-actions right\">\n              <button class=\"swipe-action-btn delete\" data-id=\"${holding.id}\" aria-label=\"Delete holding\">\n                <i data-lucide=\"trash-2\" style=\"width: 20px; height: 20px;\"></i>\n              </button>\n            </div>\n            <a href=\"#edit-asset?id=${holding.id}\" class=\"portfolio-asset-card swipe-content\" data-ticker=\"${holding.ticker}\" data-name=\"${(holding.name || '').toLowerCase()}\" style=\"display:flex; align-items:center; gap:var(--space-3); padding:var(--space-4) var(--space-4); background:var(--bg-surface); border-radius:var(--radius-xl); margin-bottom:0; border:1px solid var(--border-color); text-decoration:none; color:inherit; cursor:pointer; transition:transform 0.15s; width:100%; box-sizing:border-box;\">\n              <!-- Ticker badge -->\n              <div style=\"width:46px; height:46px; border-radius:var(--radius-lg); background:var(--bg-surface-sunken); display:flex; align-items:center; justify-content:center; flex-shrink:0; overflow:hidden;\">\n                <span style=\"font-size:0.6rem; font-weight:800; letter-spacing:0.02em; color:var(--text-secondary); text-align:center; line-height:1.2; padding:2px;\">${holding.ticker.slice(0,5)}</span>\n              </div>\n              <!-- Info -
<truncated 1356 bytes>

==================================================
Step 304 (replace_file_content)
Description: "Add swipe gesture handlers and swipe-to-delete confirmation modal click listener in PortfolioView.attachEvents."
Instruction: "Insert swipe-to-delete gesture and button action handlers immediately after search filter and before pull-to-refresh code in PortfolioView.attachEvents"
StartLine: 3488, EndLine: 3492
TargetContent:
"      });\n    }\n\n    // ── Pull-to-refresh ──────────────────────────────────"
ReplacementContent:
"      });\n    }\n\n    // Swipe Logic (v0.61)\n    const swipeContainers = container.querySelectorAll('.swipe-container');\n    swipeContainers.forEach(sw => {\n      const content = sw.querySelector('.swipe-content');\n      const actions = sw.querySelector('.swipe-actions');\n      let startX = 0;\n      let currentX = 0;\n      let isSwiping = false;\n      let threshold = 60; // minimum swipe to reveal\n\n      sw.addEventListener('touchstart', (e) => {\n        startX = e.touches[0].clientX;\n        content.style.transition = 'none';\n        isSwiping = true;\n      }, { passive: true });\n\n      sw.addEventListener('touchmove', (e) => {\n        if (!isSwiping) return;\n        currentX = e.touches[0].clientX;\n        let diff = currentX - startX;\n        \n        // Only allow swiping left (negative diff)\n        if (diff > 0) diff = 0;\n        \n        // Max reveal is the width of the actions container\n        const maxSwipe = actions.offsetWidth;\n        if (diff < -maxSwipe) {\n          // Apply resistance after reaching max reveal\n          const extra = Math.abs(diff + maxSwipe);\n          diff = -maxSwipe - (extra * 0.2);\n        }\n        \n        content.style.transform = `translateX(${diff}px)`;\n      }, { passive: true });\n\n      sw.addEventListener('touchend', (e) => {\n        if (!isSwiping) return;\n        isSwiping = false;\n        content.style.transition = 'transform 0.3s cubic-bezier(0.16, 1, 0.3, 1)';\n        \n        const diff = currentX - startX;\n        const maxSwipe = actions.offsetWidth;\n        \n        if (diff < -threshold) {\n          content.style.transform = `translateX(${-maxSwipe}px)`;\n        } else {\n          content.style.transform = 'translateX(0px)';\n        }\n      });\n    });\n\n    // Swipe Action Buttons (Delete holding)\n    container.querySelectorAll('.swipe-action-btn.delete').forEach(btn => {\n      btn.addEventListener('click', (e) => {\n        e.stopPropagation();\n        e.preventDefault();\n        const holdingId = b
<truncated 930 bytes>

==================================================
Step 334 (replace_file_content)
Description: "Set swipe container background to transparent and border-radius to match the asset card to remove gray shapes/corners in PortfolioView."
Instruction: "Add style=\"background: transparent; border-radius: var(--radius-xl);\" to the swipe-container in PortfolioView.render()"
StartLine: 3344, EndLine: 3360
TargetContent:
"          <div class=\"swipe-container\" data-id=\"${holding.id}\">\n            <div class=\"swipe-actions right\">\n              <button class=\"swipe-action-btn delete\" data-id=\"${holding.id}\" aria-label=\"Delete holding\">\n                <i data-lucide=\"trash-2\" style=\"width: 20px; height: 20px;\"></i>\n              </button>\n            </div>"
ReplacementContent:
"          <div class=\"swipe-container\" data-id=\"${holding.id}\" style=\"background: transparent; border-radius: var(--radius-xl);\">\n            <div class=\"swipe-actions right\">\n              <button class=\"swipe-action-btn delete\" data-id=\"${holding.id}\" aria-label=\"Delete holding\">\n                <i data-lucide=\"trash-2\" style=\"width: 20px; height: 20px;\"></i>\n              </button>\n            </div>"

==================================================
Step 370 (replace_file_content)
Description: "Update search filter listener in PortfolioView.attachEvents to hide parent swipe container."
Instruction: "Change search filter logic to hide/show swipe-containers instead of just portfolio-asset-cards"
StartLine: 3478, EndLine: 3491
TargetContent:
"    // Search filter\n    const searchInput = container.querySelector('#portfolio-search');\n    if (searchInput) {\n      searchInput.addEventListener('input', () => {\n        const query = searchInput.value.toLowerCase().trim();\n        const cards = container.querySelectorAll('.portfolio-asset-card');\n        cards.forEach(card => {\n          const ticker = (card.dataset.ticker || '').toLowerCase();\n          const name   = (card.dataset.name   || '').toLowerCase();\n          card.style.display = (ticker.includes(query) || name.includes(query)) ? '' : 'none';\n        });\n      });\n    }"
ReplacementContent:
"    // Search filter\n    const searchInput = container.querySelector('#portfolio-search');\n    if (searchInput) {\n      searchInput.addEventListener('input', () => {\n        const query = searchInput.value.toLowerCase().trim();\n        const swipeContainers = container.querySelectorAll('.swipe-container');\n        swipeContainers.forEach(sw => {\n          const card = sw.querySelector('.portfolio-asset-card');\n          if (card) {\n            const ticker = (card.dataset.ticker || '').toLowerCase();\n            const name   = (card.dataset.name   || '').toLowerCase();\n            const isMatch = ticker.includes(query) || name.includes(query);\n            sw.style.display = isMatch ? '' : 'none';\n          }\n        });\n      });\n    }"

==================================================
Step 374 (replace_file_content)
Description: "Format ticker badge text by removing currency/market suffixes and style it as a premium dark badge."
Instruction: "Change ticker badge html to show split cleanTicker inside a premium dark badge layout in PortfolioView.render()"
StartLine: 3345, EndLine: 3360
TargetContent:
"        const isFallback = pos.isFallback;\n\n        return `\n          <div class=\"swipe-container\" data-id=\"${holding.id}\" style=\"background: transparent; border-radius: var(--radius-xl);\">\n            <div class=\"swipe-actions right\">\n              <button class=\"swipe-action-btn delete\" data-id=\"${holding.id}\" aria-label=\"Delete holding\">\n                <i data-lucide=\"trash-2\" style=\"width: 20px; height: 20px;\"></i>\n              </button>\n            </div>\n            <a href=\"#edit-asset?id=${holding.id}\" class=\"portfolio-asset-card swipe-content\" data-ticker=\"${holding.ticker}\" data-name=\"${(holding.name || '').toLowerCase()}\" style=\"display:flex; align-items:center; gap:var(--space-3); padding:var(--space-4) var(--space-4); background:var(--bg-surface); border-radius:var(--radius-xl); margin-bottom:0; border:1px solid var(--border-color); text-decoration:none; color:inherit; cursor:pointer; transition:transform 0.15s; width:100%; box-sizing:border-box;\">\n              <!-- Ticker badge -->\n              <div style=\"width:46px; height:46px; border-radius:var(--radius-lg); background:var(--bg-surface-sunken); display:flex; align-items:center; justify-content:center; flex-shrink:0; overflow:hidden;\">\n                <span style=\"font-size:0.6rem; font-weight:800; letter-spacing:0.02em; color:var(--text-secondary); text-align:center; line-height:1.2; padding:2px;\">${holding.ticker.slice(0,5)}</span>\n              </div>"
ReplacementContent:
"        const isFallback = pos.isFallback;\n        const cleanTicker = holding.ticker.split('.')[0].split('-')[0].slice(0, 4);\n\n        return `\n          <div class=\"swipe-container\" data-id=\"${holding.id}\" style=\"background: transparent; border-radius: var(--radius-xl);\">\n            <div class=\"swipe-actions right\">\n              <button class=\"swipe-action-btn delete\" data-id=\"${holding.id}\" aria-label=\"Delete holding\">\n                <i data-lucide=\"trash-2\" style=\"width: 20px; height: 20px;\"></i>\n              </button>\n            </div>\n            <a href=\"#edit-asset?id=${holding.id}\" class=\"portfolio-asset-card swipe-content\" data-ticker=\"${holding.ticker}\" data-name=\"${(holding.name || '').toLowerCase()}\" style=\"display:flex; align-items:center; gap:var(--space-3); padding:var(--space-4) var(--space-4); background:var(--bg-surface); border-radius:var(--radius-xl); margin-bottom:0; border:1px solid var(--border-color); text-decoration:none; color:inherit; cursor:pointer; transition:transform 0.15s; width:100%; box-sizing:border-box;\">\n              <!-- Ticker badge -->\n              <div style=\"width:44px; height:44px; border-radius:12px; background:#1c1c1e; display:flex; align-items:center; justify-content:center; flex-shrink:0; overflow:hidden; box-shadow: 0 4px 10px rgba(0,0,0,0.12);\">\n                <span style=\"font-size:0.72rem; font-weight:700; color:#ffffff; text-align:center; line-height:1.1; padding:4px; text-transform:uppercase; letter-spacing:-0.02em;\">\n                  ${cleanTicker}\n                </span>\n              </div>"

==================================================
Step 408 (replace_file_content)
Description: "Update status badge rendering in PortfolioView.render to show cache timestamps when loaded via Yahoo Finance fallback without API key."
Instruction: "Change statusBadge conditional logic to render updated time and API key link when not loading but has lastUpdatedStr"
StartLine: 3262, EndLine: 3271
TargetContent:
"    // Market status badge\n    let statusBadge = '';\n    if (!hasKey) {\n      statusBadge = `<a href=\"#market-settings\" style=\"font-size:0.75rem; color:var(--color-primary); font-weight:600; text-decoration:none;\">Add API key →</a>`;\n    } else if (isLoading) {\n      statusBadge = `<span style=\"font-size:0.75rem; color:var(--text-tertiary);\">Refreshing…</span>`;\n    } else if (lastUpdatedStr) {\n      statusBadge = `<span style=\"font-size:0.75rem; color:var(--text-tertiary);\">Updated ${lastUpdatedStr}</span>`;\n    }"
ReplacementContent:
"    // Market status badge\n    let statusBadge = '';\n    if (isLoading) {\n      statusBadge = `<span style=\"font-size:0.75rem; color:var(--text-tertiary);\">Refreshing…</span>`;\n    } else if (lastUpdatedStr) {\n      statusBadge = `<span style=\"font-size:0.75rem; color:var(--text-tertiary);\">Updated ${lastUpdatedStr}</span>${!hasKey ? ` · <a href=\"#market-settings\" style=\"font-size:0.75rem; color:var(--color-primary); font-weight:600; text-decoration:none;\">Add API key →</a>` : ''}`;\n    } else if (!hasKey) {\n      statusBadge = `<a href=\"#market-settings\" style=\"font-size:0.75rem; color:var(--color-primary); font-weight:600; text-decoration:none;\">Add API key →</a>`;\n    }"

==================================================
Step 410 (replace_file_content)
Description: "Remove API key dependency from pull-to-refresh execution."
Instruction: "Allow refreshPortfolio to execute on pull release even if getApiKey() is empty"
StartLine: 3620, EndLine: 3635
TargetContent:
"          try {\n            if (window.StackdMarket && window.StackdMarket.getApiKey()) {\n              await window.StackdMarket.refreshPortfolio({ force: true });\n            }\n          } catch (err) {"
ReplacementContent:
"          try {\n            if (window.StackdMarket) {\n              await window.StackdMarket.refreshPortfolio({ force: true });\n            }\n          } catch (err) {"

==================================================
Step 412 (replace_file_content)
Description: "Remove hasKey check from mount auto-background refresh trigger in PortfolioView.attachEvents."
Instruction: "Execute mount refresh even if hasKey is false"
StartLine: 3657, EndLine: 3672
TargetContent:
"    if (hasKey && holdings.length > 0 && !isLoading) {\n      const tickers     = holdings.map(h => h.ticker);\n      const oldestAge   = window.StackdMarket.getOldestPriceAge(tickers);\n      const PRICE_TTL   = 5 * 60 * 1000;\n      const isStale     = !oldestAge || (Date.now() - new Date(oldestAge).getTime() > PRICE_TTL);\n      if (isStale) {\n        window.StackdMarket.refreshPortfolio();\n      }\n    }"
ReplacementContent:
"    if (holdings.length > 0 && !isLoading) {\n      const tickers     = holdings.map(h => h.ticker);\n      const oldestAge   = window.StackdMarket.getOldestPriceAge(tickers);\n      const PRICE_TTL   = 5 * 60 * 1000;\n      const isStale     = !oldestAge || (Date.now() - new Date(oldestAge).getTime() > PRICE_TTL);\n      if (isStale) {\n        window.StackdMarket.refreshPortfolio();\n      }\n    }"

==================================================
Step 454 (replace_file_content)
Description: "Add for attributes to labels in EditAssetView in views.js for WCAG 2.2 compliance."
Instruction: "Add for=\"...\" attribute to all form labels in EditAssetView"
StartLine: 3727, EndLine: 3788
TargetContent:
"        <div class=\"card\">\n          <!-- Ticker search -->\n          <div class=\"form-group\">\n            <label class=\"form-label\">Ticker Symbol</label>\n            <div style=\"position:relative;\">\n              <input id=\"asset-ticker\" type=\"text\" class=\"form-control\" placeholder=\"e.g. AAPL, BTC-USD\" value=\"${ticker}\" autocomplete=\"off\" autocorrect=\"off\" autocapitalize=\"characters\" spellcheck=\"false\">\n              <div id=\"ticker-search-results\" style=\"display:none; position:absolute; top:100%; left:0; right:0; background:var(--bg-surface); border:1px solid var(--border-color); border-radius:var(--radius-lg); z-index:100; max-height:200px; overflow-y:auto; box-shadow:0 8px 24px rgba(0,0,0,0.12);\"></div>\n            </div>\n            <p id=\"ticker-search-status\" style=\"font-size:0.75rem; color:var(--text-tertiary); margin-top:4px; display:none;\"></p>\n          </div>\n\n          <!-- Name -->\n          <div class=\"form-group\">\n            <label class=\"form-label\">Asset Name</label>\n            <input id=\"asset-name\" type=\"text\" class=\"form-control\" placeholder=\"e.g. Apple Inc.\" value=\"${name}\">\n          </div>\n\n          <!-- Asset Type -->\n          <div class=\"form-group\">\n            <label class=\"form-label\">Asset Type</label>\n            <select id=\"asset-type\" class=\"form-control\" style=\"appearance:none;\">\n              ${typeOptions}\n            </select>\n          </div>\n        </div>\n\n        <div class=\"card\">\n          <!-- Quantity -->\n          <div class=\"form-group\">\n            <label class=\"form-label\">Quantity (Shares)</label>\n            <input id=\"asset-quantity\" type=\"number\" class=\"form-control\" placeholder=\"0\" step=\"any\" inputmode=\"decimal\" value=\"${quantity}\">\n          </div>\n\n          <!-- Buy-in Price -->\n          <div class=\"form-group\">\n            <label class=\"form-label\">Buy-In Price (per share)</label>\n            <input id=\"asset-buy-price\" type=\"numbe
<truncated 1180 bytes>
ReplacementContent:
"        <div class=\"card\">\n          <!-- Ticker search -->\n          <div class=\"form-group\">\n            <label class=\"form-label\" for=\"asset-ticker\">Ticker Symbol</label>\n            <div style=\"position:relative;\">\n              <input id=\"asset-ticker\" type=\"text\" class=\"form-control\" placeholder=\"e.g. AAPL, BTC-USD\" value=\"${ticker}\" autocomplete=\"off\" autocorrect=\"off\" autocapitalize=\"characters\" spellcheck=\"false\">\n              <div id=\"ticker-search-results\" style=\"display:none; position:absolute; top:100%; left:0; right:0; background:var(--bg-surface); border:1px solid var(--border-color); border-radius:var(--radius-lg); z-index:100; max-height:200px; overflow-y:auto; box-shadow:0 8px 24px rgba(0,0,0,0.12);\"></div>\n            </div>\n            <p id=\"ticker-search-status\" style=\"font-size:0.75rem; color:var(--text-tertiary); margin-top:4px; display:none;\"></p>\n          </div>\n\n          <!-- Name -->\n          <div class=\"form-group\">\n            <label class=\"form-label\" for=\"asset-name\">Asset Name</label>\n            <input id=\"asset-name\" type=\"text\" class=\"form-control\" placeholder=\"e.g. Apple Inc.\" value=\"${name}\">\n          </div>\n\n          <!-- Asset Type -->\n          <div class=\"form-group\">\n            <label class=\"form-label\" for=\"asset-type\">Asset Type</label>\n            <select id=\"asset-type\" class=\"form-control\" style=\"appearance:none;\">\n              ${typeOptions}\n            </select>\n          </div>\n        </div>\n\n        <div class=\"card\">\n          <!-- Quantity -->\n          <div class=\"form-group\">\n            <label class=\"form-label\" for=\"asset-quantity\">Quantity (Shares)</label>\n            <input id=\"asset-quantity\" type=\"number\" class=\"form-control\" placeholder=\"0\" step=\"any\" inputmode=\"decimal\" value=\"${quantity}\">\n          </div>\n\n          <!-- Buy-in Price -->\n          <div class=\"form-group\">\n            <label class=\"form-label\" for=\"
<truncated 1350 bytes>

==================================================
Step 458 (replace_file_content)
Description: "Add for attributes to labels in SellAssetView in views.js for WCAG 2.2 compliance."
Instruction: "Add for=\"...\" attribute to all form labels in SellAssetView"
StartLine: 3996, EndLine: 4029
TargetContent:
"        <div class=\"card\">\n          <div class=\"form-group\">\n            <label class=\"form-label\">Quantity to Sell</label>\n            <input id=\"sell-qty\" type=\"number\" class=\"form-control\" placeholder=\"0\" step=\"any\" inputmode=\"decimal\" max=\"${availableQty}\" value=\"${availableQty}\">\n          </div>\n\n          <div class=\"form-group\">\n            <label class=\"form-label\" style=\"display:flex; justify-content:space-between;\">\n              <span>Sell Price (per share)</span>\n              ${suggestedPrice ? `<span style=\"font-size:0.75rem; color:var(--color-primary); cursor:pointer;\" id=\"use-market-price\">Use market: ${suggestedPrice}</span>` : ''}\n            </label>\n            <input id=\"sell-price\" type=\"number\" class=\"form-control\" placeholder=\"0.00\" step=\"any\" inputmode=\"decimal\" value=\"${suggestedPrice}\">\n          </div>\n\n          <div class=\"form-group\">\n            <label class=\"form-label\">Currency</label>\n            <input id=\"sell-currency\" type=\"text\" class=\"form-control\" value=\"${holding.buyInCurrency || 'USD'}\" maxlength=\"5\" autocapitalize=\"characters\">\n          </div>\n\n          <div class=\"form-group\">\n            <label class=\"form-label\">Sell Date</label>\n            <input id=\"sell-date\" type=\"date\" class=\"form-control\" value=\"${today}\">\n          </div>\n\n          ${state.accounts && state.accounts.length > 0 ? `\n          <div class=\"form-group\" style=\"margin-bottom:0;\">\n            <label class=\"form-label\">Proceeds → Account (Optional)</label>\n            <select id=\"sell-destination\" class=\"form-control\" style=\"appearance:none;\">\n              <option value=\"\">— None —</option>\n              ${accountOptions}\n            </select>\n          </div>\n          ` : ''}\n        </div>"
ReplacementContent:
"        <div class=\"card\">\n          <div class=\"form-group\">\n            <label class=\"form-label\" for=\"sell-qty\">Quantity to Sell</label>\n            <input id=\"sell-qty\" type=\"number\" class=\"form-control\" placeholder=\"0\" step=\"any\" inputmode=\"decimal\" max=\"${availableQty}\" value=\"${availableQty}\">\n          </div>\n\n          <div class=\"form-group\">\n            <label class=\"form-label\" for=\"sell-price\" style=\"display:flex; justify-content:space-between;\">\n              <span>Sell Price (per share)</span>\n              ${suggestedPrice ? `<span style=\"font-size:0.75rem; color:var(--color-primary); cursor:pointer;\" id=\"use-market-price\">Use market: ${suggestedPrice}</span>` : ''}\n            </label>\n            <input id=\"sell-price\" type=\"number\" class=\"form-control\" placeholder=\"0.00\" step=\"any\" inputmode=\"decimal\" value=\"${suggestedPrice}\">\n          </div>\n\n          <div class=\"form-group\">\n            <label class=\"form-label\" for=\"sell-currency\">Currency</label>\n            <input id=\"sell-currency\" type=\"text\" class=\"form-control\" value=\"${holding.buyInCurrency || 'USD'}\" maxlength=\"5\" autocapitalize=\"characters\">\n          </div>\n\n          <div class=\"form-group\">\n            <label class=\"form-label\" for=\"sell-date\">Sell Date</label>\n            <input id=\"sell-date\" type=\"date\" class=\"form-control\" value=\"${today}\">\n          </div>\n\n          ${state.accounts && state.accounts.length > 0 ? `\n          <div class=\"form-group\" style=\"margin-bottom:0;\">\n            <label class=\"form-label\" for=\"sell-destination\">Proceeds → Account (Optional)</label>\n            <select id=\"sell-destination\" class=\"form-control\" style=\"appearance:none;\">\n              <option value=\"\">— None —</option>\n              ${accountOptions}\n            </select>\n          </div>\n          ` : ''}\n        </div>"

==================================================
Step 462 (replace_file_content)
Description: "Add for attribute to API key input label in MarketSettingsView."
Instruction: "Add for=\"mkt-api-key-input\" to label in MarketSettingsView"
StartLine: 4158, EndLine: 4162
TargetContent:
"          <div class=\"form-group\">\n            <label class=\"form-label\">${hasKey ? 'Update API Key' : 'Enter API Key'}</label>\n            <input id=\"mkt-api-key-input\" type=\"password\" class=\"form-control\" placeholder=\"Paste your Finnhub API key\" autocomplete=\"off\" autocorrect=\"off\" spellcheck=\"false\">\n          </div>"
ReplacementContent:
"          <div class=\"form-group\">\n            <label class=\"form-label\" for=\"mkt-api-key-input\">${hasKey ? 'Update API Key' : 'Enter API Key'}</label>\n            <input id=\"mkt-api-key-input\" type=\"password\" class=\"form-control\" placeholder=\"Paste your Finnhub API key\" autocomplete=\"off\" autocorrect=\"off\" spellcheck=\"false\">\n          </div>"

==================================================
Step 466 (replace_file_content)
Description: "Add aria-label attributes to search input and performance toggle select in PortfolioView.render."
Instruction: "Add aria-label to portfolio-search input and aria-hidden to decorative search icon"
StartLine: 3420, EndLine: 3426
TargetContent:
"        <!-- Search bar -->\n        ${holdings.length > 0 ? `\n        <div style=\"position:relative; margin-bottom:var(--space-4);\">\n          <i data-lucide=\"search\" style=\"position:absolute; left:14px; top:50%; transform:translateY(-50%); width:16px; height:16px; color:var(--text-tertiary); pointer-events:none;\"></i>\n          <input id=\"portfolio-search\" type=\"text\" placeholder=\"Search assets…\" style=\"width:100%; padding:10px 14px 10px 38px; background:var(--bg-surface); border:1px solid var(--border-color); border-radius:var(--radius-lg); font-size:0.9rem; color:var(--text-primary); outline:none; box-sizing:border-box;\">\n        </div>\n        ` : ''}"
ReplacementContent:
"        <!-- Search bar -->\n        ${holdings.length > 0 ? `\n        <div style=\"position:relative; margin-bottom:var(--space-4);\">\n          <i data-lucide=\"search\" style=\"position:absolute; left:14px; top:50%; transform:translateY(-50%); width:16px; height:16px; color:var(--text-tertiary); pointer-events:none;\" aria-hidden=\"true\"></i>\n          <input id=\"portfolio-search\" type=\"text\" placeholder=\"Search assets…\" aria-label=\"Search assets\" style=\"width:100%; padding:10px 14px 10px 38px; background:var(--bg-surface); border:1px solid var(--border-color); border-radius:var(--radius-lg); font-size:0.9rem; color:var(--text-primary); outline:none; box-sizing:border-box;\">\n        </div>\n        ` : ''}"

==================================================
Step 470 (replace_file_content)
Description: "Add aria-label to performance dropdown and aria-hidden to decorative chevron-down icon."
Instruction: "Add aria-label to portfolio-perf-toggle-select and aria-hidden to chevron-down icon"
StartLine: 3432, EndLine: 3438
TargetContent:
"          <div style=\"position:relative; display:inline-flex; align-items:center; gap:4px; cursor:pointer;\">\n            <select id=\"portfolio-perf-toggle-select\" style=\"appearance:none; background:transparent; border:none; color:var(--text-secondary); font-size:0.85rem; font-weight:600; padding-right:16px; cursor:pointer; font-family:inherit; outline:none; text-align:right;\">\n              <option value=\"percentage\" ${isPercentage ? 'selected' : ''}>Percentage</option>\n              <option value=\"absolute\" ${!isPercentage ? 'selected' : ''}>Absolute Value</option>\n            </select>\n            <i data-lucide=\"chevron-down\" style=\"position:absolute; right:0; top:50%; transform:translateY(-50%); width:12px; height:12px; color:var(--text-secondary); pointer-events:none;\"></i>\n          </div>"
ReplacementContent:
"          <div style=\"position:relative; display:inline-flex; align-items:center; gap:4px; cursor:pointer;\">\n            <select id=\"portfolio-perf-toggle-select\" aria-label=\"Performance display format\" style=\"appearance:none; background:transparent; border:none; color:var(--text-secondary); font-size:0.85rem; font-weight:600; padding-right:16px; cursor:pointer; font-family:inherit; outline:none; text-align:right;\">\n              <option value=\"percentage\" ${isPercentage ? 'selected' : ''}>Percentage</option>\n              <option value=\"absolute\" ${!isPercentage ? 'selected' : ''}>Absolute Value</option>\n            </select>\n            <i data-lucide=\"chevron-down\" style=\"position:absolute; right:0; top:50%; transform:translateY(-50%); width:12px; height:12px; color:var(--text-secondary); pointer-events:none;\" aria-hidden=\"true\"></i>\n          </div>"

==================================================
Step 716 (replace_file_content)
Description: "Display API error and offline fallback text directly in Portfolio card UI."
Instruction: "Show offline fallback badge and API error text in stock card if isFallback is true"
StartLine: 3365, EndLine: 3374
TargetContent:
"                <div style=\"display:flex; align-items:center; gap:var(--space-2); margin-bottom:2px;\">\n                  <span style=\"font-weight:700; font-size:0.95rem; color:var(--text-primary); white-space:nowrap; overflow:hidden; text-overflow:ellipsis; max-width:140px;\">${holding.name || holding.ticker}</span>\n                  ${isFallback ? `<i data-lucide=\"alert-circle\" style=\"width:12px; height:12px; color:var(--text-tertiary); flex-shrink:0;\" title=\"Using buy-in price as fallback\"></i>` : ''}\n                </div>\n                <div style=\"font-size:0.78rem; color:var(--text-secondary);\">\n                  ${netQty > 0 ? `${netQty} shares · ` : ''}${displayPrice}\n                  ${isFallback && !isSoldOut ? ' <span style=\"font-size:0.7rem; color:var(--text-tertiary);\">(est.)</span>' : ''}\n                </div>\n                ${pnlLine ? `<div style=\"margin-top:2px;\">${pnlLine}</div>` : ''}"
ReplacementContent:
"                <div style=\"display:flex; align-items:center; gap:var(--space-2); margin-bottom:2px;\">\n                  <span style=\"font-weight:700; font-size:0.95rem; color:var(--text-primary); white-space:nowrap; overflow:hidden; text-overflow:ellipsis; max-width:140px;\">${holding.name || holding.ticker}</span>\n                  ${isFallback ? `<i data-lucide=\"alert-circle\" style=\"width:12px; height:12px; color:var(--color-expense); flex-shrink:0;\" title=\"${pos.error || 'Using buy-in price as fallback'}\"></i>` : ''}\n                </div>\n                <div style=\"font-size:0.78rem; color:var(--text-secondary);\">\n                  ${netQty > 0 ? `${netQty} shares · ` : ''}${displayPrice}\n                  ${isFallback && !isSoldOut ? ` · <span style=\"font-size:0.72rem; color:var(--color-expense); font-weight:500;\">Offline fallback</span>` : ''}\n                </div>\n                ${isFallback && !isSoldOut && pos && pos.error ? `<div style=\"font-size:0.68rem; color:var(--color-expense); margin-top:2px; font-weight:500;\">⚠️ ${pos.error}</div>` : ''}\n                ${pnlLine ? `<div style=\"margin-top:2px;\">${pnlLine}</div>` : ''}"

==================================================
Step 720 (multi_replace_file_content)
Description: "Apply WCAG 2.2 accessibility fixes to forms in views.js: link labels to inputs, add ARIA attributes, and keyboard access roles."
Instruction: "Apply label for, ARIA, and role attributes to form elements in views.js"
StartLine: undefined, EndLine: undefined
TargetContent:
undefined
ReplacementContent:
undefined
ReplacementChunks:
"[{\"AllowMultiple\":false,\"EndLine\":1613,\"ReplacementContent\":\"          <div class=\\\"form-group\\\">\\n            <label class=\\\"form-label\\\" for=\\\"new-cat-name\\\">Category Name</label>\\n            <input type=\\\"text\\\" id=\\\"new-cat-name\\\" class=\\\"form-control\\\" placeholder=\\\"e.g. Subscriptions\\\">\\n          </div>\\n          <div class=\\\"form-group\\\">\\n            <label class=\\\"form-label\\\" for=\\\"new-cat-icon-selector\\\">Icon</label>\\n            <div id=\\\"new-cat-icon-selector\\\" class=\\\"touch-target\\\" tabindex=\\\"0\\\" role=\\\"button\\\" aria-label=\\\"Select Category Icon\\\" style=\\\"cursor: pointer; display: flex; align-items: center; justify-content: space-between; padding: 12px; background: var(--bg-surface-sunken); border-radius: var(--radius-md); border: 1px solid var(--border-color);\\\">\\n              <span id=\\\"selected-icon-display\\\" style=\\\"color: var(--color-primary); display: flex; align-items: center; justify-content: center;\\\"><i data-lucide=\\\"${selectedIcon}\\\"></i></span>\\n              <span style=\\\"color: var(--text-tertiary); font-size: 0.8rem;\\\">Change Icon</span>\\n            </div>\\n          </div>\",\"StartLine\":1603,\"TargetContent\":\"          <div class=\\\"form-group\\\">\\n            <label class=\\\"form-label\\\">Category Name</label>\\n            <input type=\\\"text\\\" id=\\\"new-cat-name\\\" class=\\\"form-control\\\" placeholder=\\\"e.g. Subscriptions\\\">\\n          </div>\\n          <div class=\\\"form-group\\\">\\n            <label class=\\\"form-label\\\">Icon</label>\\n            <div id=\\\"new-cat-icon-selector\\\" class=\\\"touch-target\\\" style=\\\"cursor: pointer; display: flex; align-items: center; justify-content: space-between; padding: 12px; background: var(--bg-surface-sunken); border-radius: var(--radius-md); border: 1px solid var(--border-color);\\\">\\n              <span id=\\\"selected-icon-display\\\" style=\\\"color: var(--color-primary); display: flex; align-items: center; justify-content: center;\\\"><i data-lucide=\\\"${selectedIcon}\\\"></i></span>\\n              <span style=\\\"color: var(--text-tertiary); font-size: 0.8rem;\n<truncated 9682 bytes>"
